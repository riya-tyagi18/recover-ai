import { Placeholder } from "@/components/Placeholder";

export default function OverviewPage() {
  return (
    <Placeholder
      title="Overview"
      body="Phase 3: KPIs computed from the batch you run — ₹ recovered, recovery rate, funnel. Nothing here is hardcoded."
    />
  );
}

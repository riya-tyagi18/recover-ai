import { Placeholder } from "@/components/Placeholder";

export default function StrategyLabPage() {
  return (
    <Placeholder
      title="Strategy Lab"
      body="Phase 3: A/B comparison of immediate retry, delayed retry, nudge + retry, and alt-payment request — rates and ₹ from actual run data."
    />
  );
}

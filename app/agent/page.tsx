import { Placeholder } from "@/components/Placeholder";

export default function AgentPage() {
  return (
    <Placeholder
      title="Agent graph"
      body="Phase 2–4: Diagnose → probability → strategy → execute → observe & stop. Live node states on a LangGraph-style flow."
    />
  );
}
